<%= @post_images.each do |post_image| %>

<div>
  <%= image_tag post_image.get_image %> <!--image_tagの引数は必ず画像--><!--get_imageはpostコントローラーに記述したもの-->
  <p>投稿ユーザー画像：<%= image_tag 'sample-author1.jpg' %></p>
  <p>ショップ名：<%= post_image.shop_name %></p>
  <P>説明：<%= post_image.caption %></P>
  <p>ユーザーネーム：<%= post_image.user.name %></p> <!--user.nameでuserモデル内のnameカラムを呼び出している-->
</div>

<% end %>
